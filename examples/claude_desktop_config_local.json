{
  "_comment": "Local-dev variant. Use this when developing asic-mcp from a checkout (i.e. before the PyPI version is released, or to test unreleased changes). Replace the directory path with your own.",
  "mcpServers": {
    "asic": {
      "command": "uv",
      "args": [
        "run",
        "--directory",
        "/absolute/path/to/asic-mcp",
        "asic-mcp"
      ]
    }
  }
}
